# Remote Protocol

Shared, side-effect-free V1 contract helpers for Pixel Agents Hub and its per-PC reporters. It validates telemetry payloads and builds Remote Agent API paths; it intentionally contains no command-execution protocol.
